-- 그룹 함수

SELECT * 
FROM STUDENT;

SELECT MAX(STU_HEIGHT)
FROM STUDENT;

SELECT MIN(STU_HEIGHT)
FROM STUDENT;

SELECT AVG(STU_HEIGHT)
FROM STUDENT;

SELECT SUM(STU_HEIGHT)
FROM STUDENT;

SELECT MAX(STU_HEIGHT) AS MAX_HEIGHT
FROM STUDENT;
-- 컬럼명을 새로 만들어서 출력

SELECT STU_NAME AS 이름
FROM STUDENT;
-- AS는 생략이 가능, 컬럼명 뒤에 공백 필수

SELECT STU_NAME 이름
FROM STUDENT;

SELECT STU_NAME "이름 값"
FROM STUDENT;
-- 띄어쓰기가 있다면 묶어줘야 한다

